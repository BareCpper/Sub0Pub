# cross_module disabled: thread_local members in Broker<> cannot be dllexport'd on MSVC.
# Cross-DLL support requires deeper design work (Phase 4).
# add_subdirectory(cross_module)
add_subdirectory(minimal_sub0pub)